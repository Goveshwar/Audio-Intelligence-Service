# Audio-Intelligence-Service
“FastAPI-based Voice AI backend that converts speech to text using Whisper, generates responses using a language model (GPT-2), and converts responses back to speech using TTS. Supports both REST APIs and WebSocket for real-time audio processing.”
